# Script-only local configuration.
# These keys are consumed by scripts/lib/*.sh and docker compose interpolation,
# not by app.core.config.Settings.

API_HOST=127.0.0.1
API_PORT=8100
API_HOST_PORT=8100

POSTGRES_HOST_PORT=25432
POSTGRES_DB=fastapi_best_ai_architecture
REDIS_HOST_PORT=26379

WORKER_CONCURRENCY=4
WORKER_LOGLEVEL=INFO
WORKER_RECOVERY_LOOP=true

COMPOSE_PROJECT_NAME=fastapi-best-ai-architecture
