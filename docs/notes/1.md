结论：**数据已经符合要求，模型 required 文件也已经齐了；这条 notes 只保留当时的阶段 0 背景检查记录，不再作为当前实现入口。**

我刚按当前仓库状态确认了两件事：

```text
模型 required 校验：通过
音频 htdemucs-input 校验：通过
```

你的音频是：

```text
format          wav
codec           pcm_s16le
sample_rate     44100
channels        2
duration        11.899977s
valid           true
```

这正好满足计划里的数据前置条件：

```text
WAV
44100 Hz
stereo / 2 channels
```

而且 11.9 秒很适合本地 CPU 首轮验证，不是很长的完整歌曲。

**当前状态评估**

| 项目 | 状态 | 说明 |
|---|---|---|
| 模型下载 | 已满足 required | 4 个 fp32 ONNX + `bag_infer.py` + `requirements.txt` 都在 |
| 测试音频 | 已满足 | `.data/misc/2485_0003_S6_梁萧.wav` 可直接作为测试输入 |
| `audio_stem_separation` Job | 未实现 | 当前仓库没有 `app/jobs/types/audio_stem_separation/` |
| ONNX I/O 探测 | 未完成 | 计划要求先实测 4 个 ONNX 文件签名和 sha256 |
| 运行依赖 | 未接入 | `pyproject.toml` 里还没有 `onnxruntime` / `soundfile` |
| lease 修复 | 未完成 | `timeout_seconds` 仍未用于 attempt lease 窗口 |

所以不是“现在就接入完整 Job”，而是**可以开始阶段 0：探测先行**。

推荐下一步顺序：

```text
1. 探测 4 个 ONNX 专家模型的输入/输出签名和 sha256
2. 对照 bag_infer.py 确认官方聚合逻辑
3. 把探测结果写入后续 model_asset.yaml 设计
4. 再进入 schema / executor / lease 修复
```

这里还有一个文档漂移点：plan 里 `Remaining Gaps` 还写着“模型权重尚未落地”，但你现在已经下载并校验通过了。后续可以把它改成“模型 required 权重已落地，ONNX I/O 探测未完成”。

我的建议：**现在可以开始接入，但第一步只做阶段 0 探测，不要直接写 executor。** 直接写 executor 会跳过模型签名、sha256、bag 聚合逻辑确认，风险太高。
